<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title or site.title }}</title>
  <meta name="description" content="{{ description or site.description }}" />
  <link rel="stylesheet" href="{{ '/assets/css/styles.css' | url }}" />
  <link rel="icon" href="{{ '/assets/favicon.svg' | url }}" type="image/svg+xml" />
</head>
<body class="{% if ugly %}ugly{% endif %}">
  <a class="skip-nav" href="#main">Skip to content</a>

  <header class="navbar">
    <div class="container navbar-inner">
      <div class="navbarlogo"><a href="{{ '/' | url }}">Rocky Mountain Tree Trout</a></div>
      <nav class="navbarmenu">
        {% for item in site.nav %}
          <a href="{{ item.url | url }}" class="{% if page.url == item.url %}active{% endif %}">{{ item.title }}</a>
        {% endfor %}
      </nav>
    </div>
  </header>

  <div class="layout">
    <aside class="sidebar">
      <div class="box">
        <h3>Show Your Support</h3>
        <p>Become a Friend of the Rocky Mountain Tree Trout.</p>
        <p><a class="btn" href="{{ '/help/' | url }}">Help Save the Tree Trout</a></p>
      </div>
      <div class="box">
        <h3>Latest</h3>
        <p>Read <a href="{{ '/news/' | url }}">news & updates</a>.</p>
      </div>
    </aside>
    <main id="main" class="body">
      {% if page.intro %}
        <section class="intro">{{ page.intro | safe }}</section>
      {% endif %}
      {{ content | safe }}
    </main>
  </div>

  <footer>
    <div class="container">
      <section class="site-links">
        <h3>Links</h3>
        <ul>
          {% for item in footerLinks %}
            <li><a href="{{ item.url | url }}">{{ item.title }}</a></li>
          {% endfor %}
        </ul>
      </section>
    </div>
    <div class="navbar">
      <div class="container navbar-inner">
        <div class="navbarinfo">&copy; {{ year }} {{ site.author }}</div>
        <div class="navbarmenu">
          {% for item in site.nav %}
            <a href="{{ item.url | url }}">{{ item.title }}</a>
          {% endfor %}
        </div>
      </div>
    </div>
  </footer>
</body>
</html>
