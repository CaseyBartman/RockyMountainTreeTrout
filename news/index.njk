---
layout: base.njk
title: News
description: Updates on research, sightings, and conservation for the Tree Trout.
---
<h2>Latest News</h2>
<ul>
  {% for post in collections.news | reverse %}
    <li>
      <span class="tag">News</span>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <span class="muted"> · {{ post.date | readableDate }}</span>
      {% if post.data.summary %}<div class="muted">{{ post.data.summary }}</div>{% endif %}
    </li>
  {% endfor %}
</ul>
